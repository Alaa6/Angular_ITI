import { NationalIDDatePipe } from './national-iddate.pipe';

describe('NationalIDDatePipe', () => {
  it('create an instance', () => {
    const pipe = new NationalIDDatePipe();
    expect(pipe).toBeTruthy();
  });
});
