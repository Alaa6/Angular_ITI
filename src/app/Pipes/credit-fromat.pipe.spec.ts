import { CreditFromatPipe } from './credit-fromat.pipe';

describe('CreditFromatPipe', () => {
  it('create an instance', () => {
    const pipe = new CreditFromatPipe();
    expect(pipe).toBeTruthy();
  });
});
